import 'package:flutter/material.dart';

class Forth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Center(
        child: new Icon(Icons.directions_bus, size: 150.0, color: Colors.indigo[800]),
      )
    );
  }
}